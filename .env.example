# No runtime secrets are used by the current frontend preview.
# Add server-side commerce/auth values only after a backend integration exists.
# Never expose private provider keys through VITE_* variables.

